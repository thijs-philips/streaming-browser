# Producer companion for the browser-like viewer preset.
# Launch with:
#   .\scripts\run-config-demo.ps1 -ProducerConfig .\config\producer.browser.yaml `
#       -ViewerConfig .\config\viewer.browser.yaml

url: https://example.com
force_transparency: false
viewer_visible: true
alpha_probe_enabled: false
viewport:
  width: 1920
  height: 1080
frame_rate: 30
